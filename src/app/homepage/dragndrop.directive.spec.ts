import { DragndropDirective } from './dragndrop.directive';

describe('DranddropDirective', () => {
  it('should create an instance', () => {
    const directive = new DragndropDirective();
    expect(directive).toBeTruthy();
  });
});
